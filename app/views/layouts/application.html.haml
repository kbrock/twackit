!!! Strict
%html
  %head
    / %link{ :rel => "shortcut icon", :href => "/favicon.ico", :type => "image/x-icon" }
    %title
      Twackit - 
      = yield(:page_title) || 'Whatever it is, track it with Twackit!'
      
    = stylesheet_link_tag 'reset'
    = stylesheet_link_tag 'screen'
    / = javascript_include_tag :defaults

  %body  
    #page
      #header

        #report-form
          - form_tag root_path do
            http://www.twackit.com/
            = text_field_tag :twitter_username, @twitter_username || 'your Twitter username'
            = '/'
            = text_field_tag :hashtag, @hashtag || 'your twacking hashtag'
            = submit_tag 'Report'

  
        %h1= link_to 'Twackit', root_path
        %p Whatever it is, track it with Twackit!
    
      #content{ :class => controller.controller_name }
        .work-in-progress
          Thanks for visiting! Twackit is still a work in progress, but your
          %strong= link_to_function 'feedback', "UserVoice.Popin.show()"
          is appreciated.
          
          
        /= flash_messages
        = yield

      #footer
        %p
          Copyright &copy; 2009, 
          == #{link_to 'Jeremy Weiskotten', 'http://weiskotten.com'}.
          %strong= link_to_function 'Feedback', "UserVoice.Popin.show()"

    = render :partial => 'layouts/uservoice'
    = render :partial => 'layouts/google_analytics'